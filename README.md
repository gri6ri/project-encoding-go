# сервис для конвертации данных 
Данный сервис берет данные Docker-Compose файла, которые хранятся в файле ```generateInputFiles.go``` и конвертирует их:
- из JSON в YAML
- из YAML в JSON

Программа запускается через терминал командой:

```go run .```
